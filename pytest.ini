[pytest]
addopts = -ra -q
testpaths = tests
markers =
    unit: fast isolated unit tests
    integration: component interaction tests
    e2e: end-to-end pipeline tests
    security: security and adversarial tests
    hallucination: grounding and hallucination checks
